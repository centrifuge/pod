version: '3'
services:
  bridge:
    image: "chainbridgev2_bridge:latest"
    container_name: bridge
    environment:
      - KEYSTORE_PASSWORD=a
    volumes:
      - ${BRIDGE_CONFIGDIR}:/config/
      - ${BRIDGE_KEYSDIR}:/keys/
    command: --config /config/config.toml
