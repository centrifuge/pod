version: '3'
services:
  bridge:
    image: "centrifugeio/chainbridge:2022093023-5bec8a9"
    container_name: bridge
    environment:
      - KEYSTORE_PASSWORD=centrifuge
    volumes:
      - ${BRIDGE_CONFIGDIR}:/config/
      - ${BRIDGE_KEYSDIR}:/keys/
    command: --config /config/config.json --keystore /keys --verbosity dbug --latest
